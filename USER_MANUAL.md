# 📖 NSFW-GO 用户使用手册

> 从零开始，5 分钟上手 NSFW-GO

## 目录

- [第一步：获取项目](#第一步获取项目)
- [第二步：部署应用](#第二步部署应用)
- [第三步：初始配置](#第三步初始配置)
- [第四步：开始使用](#第四步开始使用)
- [常见使用场景](#常见使用场景)
- [常见问题](#常见问题)

---

## 第一步：获取项目

### 方法 1: 下载 Release 包（推荐）

1. 访问 GitHub Release 页面
2. 下载最新版本的压缩包
3. 解压到你的目录

```bash
# 示例
wget https://github.com/your-repo/NSFW-GO/archive/refs/tags/v1.0.0.tar.gz
tar -xzf v1.0.0.tar.gz
cd NSFW-GO-1.0.0
```

### 方法 2: Git 克隆

```bash
git clone https://github.com/your-repo/NSFW-GO.git
cd NSFW-GO
```

---

## 第二步：部署应用

### 🎯 推荐方式：一键部署

**适合人群**：新手用户，不熟悉 Docker 配置

```bash
# 1. 给脚本执行权限
chmod +x deploy.sh

# 2. 运行部署脚本
./deploy.sh
```

**脚本会引导你完成**：
1. ✅ 检查 Docker 环境
2. ✅ 配置数据库密码
3. ✅ 设置媒体库路径
4. ✅ 选择可选功能
5. ✅ 自动启动所有服务

**部署完成后**，脚本会显示访问地址。

---

### 🔧 高级方式：手动部署

**适合人群**：有 Docker 经验的用户

```bash
# 1. 复制配置文件
cp .env.example .env

# 2. 编辑配置（重要！）
nano .env
# 或使用你喜欢的编辑器
vim .env
```

**必须修改的配置**：

```bash
# 数据库密码（必须改！）
POSTGRES_PASSWORD=your_secure_password_here

# 媒体库路径（必须改！）
MEDIA_BASE_PATH=/path/to/your/media
```

**示例配置**：

```bash
# Linux 用户
MEDIA_BASE_PATH=/home/username/Videos/NSFW

# Synology NAS 用户
MEDIA_BASE_PATH=/volume1/media/NSFW

# macOS 用户
MEDIA_BASE_PATH=/Users/username/Movies/NSFW

# Windows WSL2 用户
MEDIA_BASE_PATH=/mnt/c/Users/username/Videos/NSFW
```

```bash
# 3. 启动服务
docker compose -f docker-compose.prod.yml up -d

# 4. 查看启动日志
docker compose -f docker-compose.prod.yml logs -f api

# 5. 等待服务就绪（约 30-60 秒）
# 看到 "Server started" 即可访问
```

---

## 第三步：初始配置

### 访问 Web 界面

打开浏览器，访问：**http://localhost:8080**

如果是远程服务器，访问：**http://your-server-ip:8080**

### 首次配置向导

#### 1️⃣ 配置媒体库扫描

访问：http://localhost:8080/config.html

**配置项说明**：

| 配置项 | 说明 | 推荐值 |
|--------|------|--------|
| 媒体库路径 | 你的影片存放目录 | 实际路径 |
| 扫描间隔 | 多久扫描一次新影片 | 15m（15分钟） |
| 支持格式 | 扫描的视频格式 | mp4,mkv,avi,mov |

**点击"保存配置"** → **点击"触发扫描"**

> 💡 首次扫描可能需要一些时间，取决于你的影片数量

#### 2️⃣ 配置 JAVDb 爬虫（可选）

如果你想自动获取影片信息：

访问：http://localhost:8080/config.html

| 配置项 | 说明 | 推荐值 |
|--------|------|--------|
| JAVDb URL | JAVDb 网站地址 | https://javdb.com |
| 排行榜爬取时间 | 自动爬取排行榜的时间 | 0 12 * * *（每天12点） |
| 代理设置 | 如需代理访问 | http://proxy:port |

**点击"保存配置"** → **点击"测试连接"**

#### 3️⃣ 配置种子下载（可选）

如果你想使用种子下载功能：

**前提条件**：
- 已安装并配置 Jackett
- 已安装并配置 qBittorrent

访问：http://localhost:8080/config.html

**Jackett 配置**：
```
URL: http://your-jackett-host:9117
API Key: 在 Jackett 界面获取
```

**qBittorrent 配置**：
```
URL: http://your-qbittorrent-host:8085
用户名: admin
密码: your_password
下载目录: /downloads（或 Synology 的 /volume1/Downloads）
```

**点击"测试连接"** 确保配置正确

---

## 第四步：开始使用

### 🏠 主页 - 系统概览

访问：http://localhost:8080/

**你可以看到**：
- 📊 总影片数量
- 📊 本地影片数量
- 📊 演员数量
- 📊 制片厂数量

### 🎬 本地影片管理

访问：http://localhost:8080/local-movies.html

**功能**：
- ✅ 浏览本地影片列表
- ✅ 查看影片封面和信息
- ✅ 筛选和排序
- ✅ 手动触发扫描
- ✅ 查看影片详情

**使用技巧**：
1. 点击封面图查看大图
2. 点击影片标题查看详情
3. 使用顶部筛选器快速查找
4. 点击"刷新"按钮重新扫描

### 🔍 搜索功能

访问：http://localhost:8080/search.html

**两种搜索方式**：

1. **本地搜索**
   - 搜索已下载的影片
   - 按番号、演员、标题搜索
   - 即时结果

2. **在线搜索（JAVDb）**
   - 搜索 JAVDb 数据库
   - 查看影片详情
   - 一键下载种子

**使用示例**：
```
搜索：STARS-123
结果：显示该番号的所有信息
操作：查看详情 → 下载种子 → 添加到 qBittorrent
```

### 📈 排行榜

访问：http://localhost:8080/rankings.html

**功能**：
- ✅ 查看 JAVDb 热门排行
- ✅ 按日/周/月筛选
- ✅ 查看本地是否已有
- ✅ 一键下载种子

**使用流程**：
1. 选择排行榜类型（日榜/周榜/月榜）
2. 浏览排行榜列表
3. 绿色标记 = 本地已有
4. 点击"下载"按钮获取种子

### 💾 下载管理

访问：http://localhost:8080/downloads.html

**功能**：
- ✅ 查看下载任务
- ✅ 查看下载进度
- ✅ 暂停/恢复下载
- ✅ 删除任务

### ⚙️ 系统配置

访问：http://localhost:8080/config.html

**可配置项**：
- 媒体库设置
- 爬虫设置
- 下载器设置
- 系统参数

### 📝 系统日志

访问：http://localhost:8080/logs.html

**功能**：
- ✅ 查看系统运行日志
- ✅ 按级别筛选（错误/警告/信息）
- ✅ 搜索日志内容
- ✅ 清空日志

---

## 常见使用场景

### 场景 1️⃣：添加新影片到媒体库

**步骤**：

1. 将影片文件复制到媒体库目录
   ```bash
   cp movie.mp4 /path/to/your/media/
   ```

2. 等待自动扫描（默认 15 分钟）

   或手动触发扫描：
   - 访问 http://localhost:8080/local-movies.html
   - 点击"刷新"按钮

3. 在本地影片列表中查看新影片

**自动化流程**：
- 系统每 15 分钟自动扫描一次
- 自动识别番号
- 自动匹配封面和信息

---

### 场景 2️⃣：搜索并下载新影片

**步骤**：

1. 访问搜索页面
   http://localhost:8080/search.html

2. 选择"在线搜索"标签

3. 输入番号或关键词
   ```
   例如：STARS-123
   或：上原亚衣
   ```

4. 点击搜索，浏览结果

5. 点击影片的"下载"按钮

6. 选择种子文件

7. 自动添加到 qBittorrent

8. 下载完成后自动扫描到本地库

---

### 场景 3️⃣：查看热门排行榜

**步骤**：

1. 访问排行榜页面
   http://localhost:8080/rankings.html

2. 选择排行榜类型
   - 日榜：今日热门
   - 周榜：本周热门
   - 月榜：本月热门

3. 浏览列表
   - 绿色标记：本地已有
   - 无标记：未下载

4. 点击"下载"获取种子

5. 等待下载完成

---

### 场景 4️⃣：批量下载排行榜

**步骤**：

1. 访问排行榜
   http://localhost:8080/rankings.html

2. 选择要下载的影片

3. 点击每个影片的"下载"按钮

4. 访问下载管理页面
   http://localhost:8080/downloads.html

5. 监控下载进度

**高级技巧**：
- 在 qBittorrent 中设置下载完成后移动到媒体库
- NSFW-GO 会自动扫描新文件
- 实现全自动化流程

---

### 场景 5️⃣：管理本地影片

**步骤**：

1. 访问本地影片页面
   http://localhost:8080/local-movies.html

2. 使用筛选功能
   - 按演员筛选
   - 按制片厂筛选
   - 按标签筛选

3. 查看影片详情
   - 点击影片标题
   - 查看完整信息

4. 管理影片
   - 删除影片（从数据库移除）
   - 重新扫描（更新信息）

---

## 常见问题

### ❓ 影片扫描失败？

**可能原因**：
1. 媒体库路径配置错误
2. 文件权限问题
3. 文件命名不规范

**解决方案**：
```bash
# 1. 检查路径
docker exec nsfw-api ls -la /app/media

# 2. 检查权限
sudo chmod -R 755 /path/to/your/media

# 3. 检查文件命名
# 推荐格式：STARS-123.mp4
# 或：[ThZu.Cc]stars-123.mp4
```

### ❓ 无法下载种子？

**检查清单**：
- [ ] Jackett 是否运行？
- [ ] qBittorrent 是否运行？
- [ ] 配置是否正确？
- [ ] 网络是否通畅？

**测试连接**：
1. 访问配置页面
2. 点击"测试连接"按钮
3. 查看错误信息

### ❓ 爬虫无法访问 JAVDb？

**可能原因**：
1. 网络限制
2. 需要代理
3. JAVDb 域名变更

**解决方案**：
```bash
# 在 .env 中配置代理
CRAWLER_PROXY=http://your-proxy:8080

# 或使用 SOCKS5 代理
CRAWLER_PROXY=socks5://your-proxy:1080

# 重启服务
docker compose -f docker-compose.prod.yml restart api
```

### ❓ 服务占用内存过高？

**优化配置**：

编辑 `.env` 文件：
```bash
# 减少数据库连接池
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=5

# 增加扫描间隔
MEDIA_SCAN_INTERVAL=30m
```

重启服务：
```bash
docker compose -f docker-compose.prod.yml restart
```

### ❓ 如何备份数据？

**备份数据库**：
```bash
# 导出数据库
docker exec nsfw-postgres pg_dump -U nsfw nsfw_db > backup.sql

# 恢复数据库
docker exec -i nsfw-postgres psql -U nsfw -d nsfw_db < backup.sql
```

**备份配置**：
```bash
# 备份 .env 和配置文件
cp .env .env.backup
cp -r configs configs.backup
```

### ❓ 如何升级版本？

**升级步骤**：
```bash
# 1. 备份数据
docker exec nsfw-postgres pg_dump -U nsfw nsfw_db > backup.sql

# 2. 停止服务
docker compose -f docker-compose.prod.yml down

# 3. 拉取新版本
git pull origin main

# 4. 重新构建
docker compose -f docker-compose.prod.yml build --no-cache

# 5. 启动服务
docker compose -f docker-compose.prod.yml up -d

# 6. 检查日志
docker compose -f docker-compose.prod.yml logs -f api
```

---

## 🎓 进阶使用

### 启用管理工具

**pgAdmin（数据库管理）**：
```bash
docker compose -f docker-compose.prod.yml --profile admin up -d
```
访问：http://localhost:5050

**Redis Commander**：
```bash
docker compose -f docker-compose.prod.yml --profile admin up -d
```
访问：http://localhost:8081

### 启用监控

**Grafana + Prometheus**：
```bash
docker compose -f docker-compose.prod.yml --profile monitoring up -d
```
访问：
- Grafana: http://localhost:3000
- Prometheus: http://localhost:9090

### API 使用

NSFW-GO 提供 RESTful API：

**获取本地影片**：
```bash
curl http://localhost:8080/api/v1/local/movies?page=1&limit=20
```

**搜索影片**：
```bash
curl http://localhost:8080/api/v1/search/javdb?q=STARS-123
```

**获取统计信息**：
```bash
curl http://localhost:8080/api/v1/stats
```

详细 API 文档：http://localhost:8080/swagger（如果启用）

---

## 📞 获取帮助

**问题反馈**：
- GitHub Issues: https://github.com/your-repo/NSFW-GO/issues

**文档**：
- 快速指南：[DEPLOY_GUIDE.md](DEPLOY_GUIDE.md)
- 部署文档：[README.docker.md](README.docker.md)
- 开发文档：[CLAUDE.md](CLAUDE.md)

**日志查看**：
```bash
# API 日志
docker compose -f docker-compose.prod.yml logs -f api

# 所有服务日志
docker compose -f docker-compose.prod.yml logs -f
```

---

## 🎉 享受使用！

你现在已经掌握了 NSFW-GO 的所有基本功能！

**记住**：
- 📂 定期备份数据库
- 🔄 定期检查更新
- 📊 监控系统资源
- 🔒 保护好你的密码

祝你使用愉快！🚀
